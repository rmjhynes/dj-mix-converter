# DJ Mix Converter

A script to convert `.wav` mix recordings from Rekordbox to `.mp3` to significantly reduce the size of the files.

## Prerequisites
Ensure `ffmpeg` is installed.

## Usage
`bash dj-mix-converter.sh`
